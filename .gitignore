# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Local Claude Code settings
.claude/

# scratch
*.tmp

# Content Desk hand-off artifacts (regenerated every run; never committed)
.desk-run/

# Tools Desk hand-off artifacts (regenerated every run; never committed)
.tools-run/

# Dev-only UI audit (Playwright) — output + the ephemeral dev install. Never shipped. The
# tracked root package.json (serverless deps) stays clean: install playwright with --no-save.
.ui-audit/
/node_modules/
/package-lock.json

# Transient harvest output (regenerated every run by scripts/collectors/ingest.py)
scripts/collectors/harvest-digest.md
scripts/collectors/harvest.json
scripts/collectors/clusters-digest.md
scripts/collectors/sources-suggestions.md
scripts/collectors/signals-digest-full.md
scripts/collectors/gaps-digest.md

# Design feedback loop (dev-only tooling — not part of the site)
shots/
shot*.py
functest.py
reports_probe.py

# stray archives
ai-daily-report.tar.gz
*.bundle

.vercel

# MCP server deps (installed locally by whoever runs it)
mcp/node_modules/
mcp/package-lock.json
