# Digest — AI tools, skills & techniques (`ai-tooling`)

> Domain scope: AI models/agents/tools, new Claude features/skills/connectors,
> new skills & techniques (with how-to), research, and — for now — finance/markets
> & policy as they bear on AI/tech. Shared mission + sourcing standard live in
> [`_house.md`](./_house.md); read that first, then this file.

## How to use this file (instructions for each run)
1. **Before writing:** read `_house.md` + the "Active threads" and "Durable
   lessons" below so today's entry advances the story and doesn't repeat.
2. **After writing:** update "Active threads" (add/advance/close), add any genuinely
   new "Durable lesson," and append to the "Changelog" with one line.
3. Keep it under ~150 lines. When a thread goes quiet for 2+ weeks, move its
   one-line conclusion into "Durable lessons" and delete the thread.

---

## Active threads (ongoing storylines to advance, not repeat)
<!-- one line each: thread → latest state + date. Update/close as they evolve. -->
- **The agent grew up** → autonomy gained money (agentic payments: AWS AgentCore Payments, OpenAI ACP) + a reliability reckoning (<2% of enterprises at full prod scale). (as of Jun 6) **Advanced 2026-06-27 (autonomy reaches the screen):** Google moved **"computer use" into mainline Gemini 3.5 Flash** (`gemini-3.5-flash`, Jun 24) as a built-in tool — one fast/cheap model that sees a screen and acts (click/type/scroll/navigate/drag) across browser/mobile/desktop, public preview via the Gemini API; **78.4 OSWorld-Verified** (vs GPT-5.5 78.7) at **$1.50/$9 per M tok** (~⅓ of GPT-5.5). Run-it-today via a Browserbase zero-code demo + an official **Docker-sandboxed** reference repo (Playwright action loop). The portable lesson is the **safety scaffold**, not the model: indirect **prompt injection** is the live threat (real attacks already reported) → layer sandbox + website allowlist + confirm-before-sensitive-action + injection-auto-halt (both safeguards OFF by default) + human-in-loop + logging; Google: "no individual safeguard is sufficient." Card: card-ai-tooling-gemini-computer-use (agent-autonomy/safety-scaffold). Prices/benchmarks directional. Watch for a 2nd lab to ship a fast-tier computer-use model.
- **Recursive self-improvement goes public** → Anthropic's "When AI builds itself" (Jun 4): Claude authors >80% of its own codebase contributions; coordinated-pause proposal. (Jun 6)
- **US federal AI regime forming** → Trump EO Jun 2 ("voluntary" 30-day pre-release review for "covered frontier models") + bipartisan Great American AI Act draft Jun 4 (3-yr preemption of state DEV laws, audits >$500M rev). State-vs-federal collision is the watch-item. (Jun 6)
- **Open-weight price pressure → a tier, not a model** → DeepSeek V4 (~100× cheaper output) + MiniMax M3 (open-weight, 1M context, SWE-Bench Pro 59 claim) (Jun 6). **Advanced 2026-06-19:** Z.ai's **GLM-5.2** (Jun 16–17) took the #1 open-weights spot on the Artificial Analysis Intelligence Index — MIT-licensed, ~1M context, reportedly matches/beats GPT-5.5 on long-horizon coding at ~1/6th cost — and drops into Claude Code/Cline/Cursor with two env vars. The thread is now 'near-frontier + trivially routable,' so the **model-router** lesson is the play: bulk/long jobs → GLM-5.2, hardest agentic → Opus/GPT-5.5. Provenance caveat: hosted Z.ai routes through China infra → self-host MIT weights or use a Western host for sensitive work. Card: card-ai-tooling-glm-5-2-route (thread-open-weight-price-pressure). Prices/benchmarks directional. (2026-06-19) **Advanced 2026-07-02 (hosted near-frontier tier moves up):** Anthropic shipped **Claude Sonnet 5** (`claude-sonnet-5`, Jun 30) — most agentic Sonnet yet, higher-effort perf 'close to Opus 4.8' on many tasks (independent agentic-coding read 63.2% vs Opus 4.8 69.2%, Sonnet 4.6 58.1%) at **$3/$15 per M tok** (~40% under Opus $5/$25), intro **$2/$10 through Aug 31 2026**, default on Free/Pro + in Claude Code/API. The router now has three tiers: GLM-class open-weight for bulk you self-host, **Sonnet 5 for hosted near-frontier at mid-tier price**, Opus for the hardest ~10%. The play is still the standing-eval router move, NOT repricing on the launch benchmark. Card: card-ai-tooling-sonnet-5-default. Resolved the Jun-6 'Sonnet refresh reprices the mid-tier' call ✅ (caveat: Sonnet 5 full bump, not the guessed 4.8). Prices/benchmarks directional. **Advanced 2026-07-24 (the cheap tier competes on TOKENS-PER-TASK, not sticker price):** Google shipped **Gemini 3.6 Flash** (`gemini-3.6-flash`, Jul 21) — output $1.50/$7.50 (out down from $9), but the headline is **token efficiency**: ~17% fewer output tokens on the AA Index (up to ~65% on DeepSWE) → effective **cost-per-completed-task ~31% lower** (up to ~71% agentic coding), AA Intelligence Index ~50. Siblings: **Flash-Lite** ($0.30/$2.50, index ~36) for high-throughput; **Flash Cyber** (find/validate/patch vulns, gov/trusted-partners only, no self-serve). The router move sharpens: judge candidates on **cost-per-FINISHED-task**, not per-token price, because that's where the efficiency win lands. Card: card-ai-tooling-gemini-3-6-flash-route. Flash Cyber = another gov-gated security-tuned frontier variant (read with news: OpenAI's cyber model + the lab-security race). Confirmed ≥3 (blog.google/DeepMind, 9to5Google, Artificial Analysis, buildfastwithai). Prices/benchmarks directional. (2026-07-24)
- **Compute/power = the moat** → ByteDance ~$70B infra, Anthropic ~$200B cloud/chips; capital + talent (Karpathy→Anthropic) concentrating at perceived leaders. (Jun 6)
- **Memory/"dreaming" is now table stakes** → ChatGPT Dreaming V3 (Jun 4) made async memory-synthesis the default in consumer chat (~5× cheaper). (Jun 6)
- **Hyperscalers decoupling from single model suppliers** → Microsoft in-house MAI models (Jun 2); Copilot model-agnostic. (Jun 6)
- **Capital rotating beyond LLMs** → embodied AI (Generalist $400M) + neuromorphic efficiency (Flourish $500M). (Jun 6)
- **Multi-agent orchestration becomes a built-in primitive** → Claude Code dynamic `/workflows` (research preview, late May) writes+runs an orchestration script across subagents from a plain-English "...workflow" request; June added nested subagents (5 levels). The planner→worker→verifier→merge shape is now product, not hand-wiring. Card: card-ai-tooling-claude-workflows. Watch for a 2nd major coding agent (Cursor/Gemini CLI/Copilot) to ship a comparable primitive. (2026-06-11) **Advanced 2026-06-23 (the loop side):** Claude Code's `/loop` (cron or self-paced reruns) + `/goal` (keep working until a SEPARATE cheap evaluator — defaults to Haiku — confirms a provable "done when" condition) make "loop engineering" (Claire Vo, Jun 17; Addy Osmani's heartbeat/cron/hook/goal taxonomy, Jun 7) a run-it-today primitive: define the loop once, walk away. Session-scoped (fires only while Claude Code is open; recurring tasks expire after 7 days → graduate to cloud Routines/GitHub Actions for machine-off). Cross-tool parallel to Codex's `/goal`. Card: card-ai-tooling-claude-loop-goal. Watch for the 7-day recurring-task expiry to relax / an in-CLI renew.

## Durable lessons (the compounding edge — evergreen takeaways we've taught)
<!-- promote a thread's conclusion here once it's a stable, reusable principle. -->
- **Model choice = two independent gates:** (1) capability/cost (passes your eval at acceptable price?), (2) jurisdiction/provenance (where do weights live, what regulation attaches if you fine-tune?). A model can pass one and fail the other.
- **Keep a "model router":** map each recurring task to the cheapest model that passes a standing eval of 3–5 of your real tasks; re-check monthly — rankings/prices move weekly.
- **Treat unverified benchmark claims as hypotheses with a verification date,** never reprice your stack on a pre-release number.
- **Durable execution beats model IQ in prod:** checkpoint each step, make external actions idempotent, replay from last checkpoint. (Also stops a retried agent double-paying.)
- **Give spending agents a corporate card, not a blank check:** hard budget + per-tx cap + endpoint allowlist + human approval above a threshold + idempotency keys + audit log. (x402)
- **Build compliance artifacts once** (model card + eval suite + red-team + incident runbook), emit per-jurisdiction exports (EU AI Act, US EO/CAISI).
- **Planner → worker → verifier → merge** is the reliable multi-agent shape; never let one session do everything (context pollution).
- **The durable skills compound with stronger models:** orchestration, evals, judgment, context engineering, writing Skills (markdown runbooks).
- **Controls collapse → govern the system, don't operate it (weekly synthesis, Jun 1–7):** vendors/platforms keep retiring manual levers for AI-on defaults. The edge is the harness around the default — router + standing eval to pick/judge models, a persistent-memory loop and a conventions file so context is stated once, grounded Q&A over your own docs — plus one guarded manual control to prove the AI's lift before trusting it. The five Jun 1–7 dailies (Ollama, two-gate picker, release-day verdict kit, memory loop, NotebookLM) are one personal AI operating system, not five tips.

## Changelog (one line per run — newest first)
- 2026-07-24: Deep beat (longest-gone non-web = graphic + ai-tooling; ai-tooling last 07-12) — **Gemini 3.6 Flash** (`gemini-3.6-flash`, Jul 21): the cheap tier now competes on **tokens-per-task**, not sticker price — ~17% fewer output tokens (up to ~65% on coding evals) → effective **cost-per-completed-task ~31% lower** (up to ~71% agentic coding); output $1.50/$7.50; AA Index ~50. + **Flash-Lite** ($0.30/$2.50) for high-throughput + **Flash Cyber** (vuln find/patch, gov/trusted-partners only). Run-it-today = A/B behind one gateway on 3–5 real tasks, judge **cost-per-FINISHED-task**, re-point the cheap lane. Advanced thread-open-weight-price-pressure (the router move); reinforced 'keep a model router' + 'don't reprice on the launch benchmark' lessons. Card: card-ai-tooling-gemini-3-6-flash-route. Logged a prediction (a 2nd lab ships a fast-tier update whose headline win is token-efficiency/cost-per-task, not just a lower per-token price). Prices/benchmarks directional. Sources: blog.google/DeepMind, 9to5Google, Artificial Analysis, buildfastwithai.
- 2026-07-12: Weekly Issue #7 — **GPT-Live** (voice with live search + GPT-5.5-in-background) carried as the AI keeper alongside the **Grok 4.5 / Muse Spark 1.1 router move**; the ai-tooling instance of "**take the upgrade, run the one cheap proof**" → the machine now speaks with current data and vendors claim 'Opus-class at a third of the price,' so verify every fact GPT-Live fetches (keep confidential briefs off it; no API/enterprise path yet), and A/B every new 'beats Opus' model behind ONE gateway on your real tasks + wait for a third-party leaderboard before repricing. Reinforces the "model router" + "knowledge-freshness + human verify gate" durable lessons. PDF: weekly-ai-report-2026-07-12.pdf.
- 2026-07-10: Deep beat (least-recently-covered non-web, tied at 07-02; richest Confirmed harvest) — **GPT-Live** (OpenAI, Jul 8) replaces ChatGPT's old Advanced Voice Mode (a ~2024-cutoff model): **full-duplex** (listens + speaks at once → interrupt naturally, pause and it waits), backchannels, and **silently delegates to GPT-5.5 in the background** for web search/reasoning while it keeps talking — so voice finally has current knowledge + live search + memory. Run-it-today play: hands-free brainstorm/research-interview partner; tap the Voice button (Free = GPT-Live-1 mini, paid = GPT-Live-1; iOS/Android/web/CarPlay). Honest scope: **NO video/screen-share/vision at launch** (that's legacy voice on the old model), **NO API yet**, **excluded from Business/Enterprise/Edu** → keep confidential briefs off it; verify delegated facts. New card: card-ai-tooling-gpt-live (thread-ai-voice-agents). Confirmed ≥4 (OpenAI + Simon Willison + TechCrunch + TestingCatalog + 9to5Mac). Logged a prediction (GPT-Live gains video/screen-share AND/OR a realtime API by ~Sep 30). Reinforces "the tool is only as good as its knowledge freshness + the human verify gate."
- 2026-07-05: Weekly Issue #6 — **Claude Sonnet 5 (the router move)** carried as the week's AI keeper; the ai-tooling instance of the throughline "**read the gauge, feed one clean input**" → the gauge is your OWN eval (don't reprice on the launch benchmark); re-run 3–5 real tasks, re-point the router to whatever passes, reserve Opus for the hardest ~10%, and bank the $2/$10 intro (ends Aug 31) with prompt caching/batch. Resolved the Jun-6 mid-tier-repricing call ✅; the hosted-near-frontier companion to the GLM-5.2 open-weight route (thread-open-weight-price-pressure).
- 2026-07-02: Deep beat (longest-gone non-web pair = ai-tooling + growth, both 06-27) — **Claude Sonnet 5** (Anthropic, Jun 30): near-Opus-4.8 agentic quality at ~40% lower price ($3/$15 std, intro $2/$10 to Aug 31), default on Free/Pro + Claude Code/API. Run-it-today = re-run your standing eval on 3–5 real tasks, then re-point the router (bulk/agentic → Sonnet 5, hardest ~10% → Opus) — don't reprice on the headline. Advanced thread-open-weight-price-pressure (added the hosted near-frontier tier alongside GLM-5.2). Card: card-ai-tooling-sonnet-5-default. Resolved the Jun-6 mid-tier-Sonnet-refresh call ✅ (caveat: full v5 bump, not the guessed 4.8; ~1wk past window). Logged a prediction ($2/$10 intro made permanent OR a 2nd lab cuts a comparable mid-tier model to ≤~$3/$15). Sources: Anthropic, The New Stack, VentureBeat, IT Pro, MarkTechPost, Claude Platform pricing.
- 2026-06-28: Weekly Issue #5 — **Gemini 3.5 Flash computer-use** + Claude Code **/loop & /goal** carried as keepers; the spine of the week's throughline "**autonomy with a leash**" — hand the agent the doing (click the GUI, run unattended) but keep the gate (Docker sandbox + allowlist + injection-auto-halt; a separate cheap evaluator confirms `/goal`'s done-condition). Pairs with the news read: frontier models now arrive **gated** (Mythos restricted, GPT-5.6 customer-by-customer) → keep an open-weight (GLM-class) fallback you can run yourself.
- 2026-06-27: Deep beat (tie-break vs email, won on harvest richness; longest-gone-non-web pair = growth + {email,ai-tooling} both 06-24) — **Gemini 3.5 Flash Computer Use** (Google, Jun 24): "computer use" moved into mainline `gemini-3.5-flash` as a built-in tool (browser/mobile/desktop control), public preview via Gemini API, 78.4 OSWorld-Verified at $1.50/$9 per M tok (~⅓ GPT-5.5). Run-it-today = automate API-less GUI grunt work (dashboard scraping, form fill, UI smoke-tests) inside a Docker sandbox + allowlist + injection-auto-halt. Card: card-ai-tooling-gemini-computer-use; advanced the agent-autonomy thread (autonomy reaches the screen; the play is the safety scaffold vs prompt injection). Sources: Google (blog.google), Gemini API docs, The Next Web, Search Engine Journal (security). Logged a prediction (2nd lab ships a fast-tier computer-use model ≤~$2/$10 by Q3-end). Context (not the play): GPT-5.6 Sol/Terra/Luna ships govt-gated preview — see news.
- 2026-06-23: Deep beat (longest-gone non-web) — Claude Code `/loop` + `/goal` = "loop engineering": define an agent loop once (cron or self-paced) and let `/goal` run until a separate cheap (Haiku) evaluator confirms a provable done-when condition; reusable `.claude/loop.md`; session-scoped + 7-day expiry. Card: card-ai-tooling-claude-loop-goal; advanced the multi-agent-orchestration thread (cross-tool with Codex `/goal`). Sources: Claude Code Docs ×2, Lenny's/Claire Vo, ChatPRD. Logged a prediction (Anthropic relaxes the 7-day recurring-task expiry or ships an in-CLI renew by Q3-end).
- 2026-06-19: Deep beat (longest-gone non-web) — **GLM-5.2** (Z.ai, Jun 16–17) is the new #1 open-weights model (Artificial Analysis Intelligence Index), MIT, ~1M context, ~1/6th GPT-5.5 cost on long-horizon coding; run-it-today = route bulk/long jobs to it inside Claude Code via two env vars (model-router play). Card: card-ai-tooling-glm-5-2-route; advanced the open-weight-price-pressure thread. Sources: Artificial Analysis, OpenRouter, apidog, CryptoBriefing (prices/benchmarks directional; VentureBeat 403, not cited). Logged a prediction (GLM-5.2 top-3 by token volume on OpenRouter's coding leaderboard by ~Jul 19).
- 2026-06-15: Deep beat — Claude **Agent SDK billing split landed** (Jun 15): Agent SDK, `claude -p`, third-party Agent-SDK apps + **Claude Code GitHub Actions** stop drawing on the Claude plan → separate monthly credit ($20 Pro / $100 Max 5x / $200 Max 20x), metered at full API rates, **one-time opt-in you must claim**; past the credit, jobs halt unless usage credits are enabled (interactive TUI/IDE + claude.ai unaffected). Run-it-today: claim the credit + put a spend cap in front of any agent/CI workload. Card: card-ai-tooling-agent-sdk-credit (ties to card-webdev-ai-gateway-spend-limits + card-ai-tooling-model-portability). The "govern AI spend, not just consume it" instance of the controls-collapse lesson, now with a hard deadline. Sources: Claude Help Center, The New Stack, XDA. Logged a prediction (in-product Agent SDK spend dashboard/cap follows).
- 2026-06-11: Deep beat — Claude Code dynamic `/workflows` multi-agent orchestration (research preview late May + nested subagents in June) turns the planner→worker→verifier→merge lesson into a built-in primitive. Card: card-ai-tooling-claude-workflows; added the multi-agent-orchestration thread; logged a prediction (2nd coding agent ships a comparable workflow primitive). Sources: Anthropic Claude Code, Claude Code Docs, InfoQ. Note: Agent SDK/Actions metering split lands Jun 15.
- 2026-06-07: Weekly Issue #3 (Week of Jun 1–7) — synthesized the week's AI plays into one "personal AI operating system" + the cross-domain "controls collapse → govern the system" throughline; logged weekly prediction (≥2 more platforms retire manual campaign types by Sep 30). PDF: weekly-ai-report-2026-06-07.pdf.
- 2026-06-06: restructured archive to Monday–Sunday weeks; weekly now written on Monday for the prior full week. Issue #1 = Week of May 18–24 (Google I/O), Issue #2 = Week of May 25–31. Added Mon May 18 daily. Sidebar derives week grouping from sortDate.
- 2026-06-06: digest seeded from May 19 – Jun 6 reports (backfill + autonomous runs).
- 2026-06-06: ledger split into per-domain digests under docs/knowledge/digest/; this file inherited the AI/tech/markets threads, lessons, and changelog. Shared mission + sourcing moved to _house.md.
